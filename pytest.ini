[pytest]
testpaths = tests
addopts = -q
# The dl_env environment carries a third-party pytest plugin (opik) whose
# import is broken against the installed pydantic_settings. Nothing here
# needs it, and an unrelated plugin must not be able to stop the regression
# suite from running.
filterwarnings =
    ignore::UserWarning:pandas.core.computation.expressions
